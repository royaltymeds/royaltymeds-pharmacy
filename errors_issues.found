1. some of the restock modals the table texts overlap each other instead of to either use up the available space or word wrap to avoid overlapping (prefer to use up available space then word wrap if no more space to use)