# TASKS — Final Completion Log

Date finalized: 2026-05-16

## 1) Search feature in restock management tabs must search requests/items (not supplier name only) and tabs should have appropriate filters

### Implementation Completed
- In `components/admin/restock/restock-workflow-tabs.tsx`:
  - Added combined supplier-group filtering via `filterRequestGroups(...)` that matches request/item fields:
    - request number
    - supplier name
    - item product name
    - item product type
    - item notes
  - Applied that group filtering to Current Requests and Request History supplier-card lists.
  - Added tab-specific filters:
    - Current Requests: all / requested / linked_to_po / linked to placed PO
    - Request History: all / received / cancelled
    - Schedule: all / with open scheduled PO / without open scheduled PO

### Verification
- Search input filters supplier cards by contained request/item matches in Current Requests and Request History.
- Tab-specific select filters further constrain shown supplier groups/requests according to selected option.
- Lint passed after implementation.

## 2) Current requests linked to placed POs should show “Order placed” indicator (without changing request status)

### Implementation Completed
- In `components/admin/restock/restock-workflow-tabs.tsx`:
  - Added `purchaseOrdersById` map and `requestLinkedToPlacedPo(...)` helper.
  - Displayed separate “Order placed” indicator in request detail modal.
  - Added “Order placed” badge in request rows within supplier-request modal list.

### Verification
- For requests linked to `placed` PO, request status label remains unchanged while separate “Order placed” badge/indicator appears.
- Lint passed after implementation.
