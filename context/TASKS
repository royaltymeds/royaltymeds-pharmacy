# TASKS - Restock & Purchase Order Improvements

## Task 1: Add ability to hold items in purchase order details modal ✅ COMPLETED

**What was implemented:**
- Added `handlePutPurchaseOrderItemOnHold` function in [restock-workflow-tabs.tsx](components/admin/restock/restock-workflow-tabs.tsx#L349-L363)
- Added "Hold item" button in the purchase order details modal that only appears when PO status is 'open' or 'placed'
- The button allows users to move individual items from a purchase order to the on-hold queue
- When clicked, the button calls `putRestockItemOnHold` action and displays a toast notification

**Code location:**
- Button implementation: [restock-workflow-tabs.tsx lines 1424-1433](components/admin/restock/restock-workflow-tabs.tsx#L1424-L1433)
- Handler function: [restock-workflow-tabs.tsx lines 350-363](components/admin/restock/restock-workflow-tabs.tsx#L350-L363)

**How to verify:**
1. Navigate to Admin > Restock Management
2. Click on any open or placed Purchase Order to open the details modal
3. Scroll to the items list - you should see a "Hold item" button on each row
4. Click the "Hold item" button on any item
5. Verify: The item is moved to the Purchase Orders > On Hold tab and a success toast appears

---

## Task 2: Update restock management tab counts ✅ COMPLETED

**What was implemented:**
- Modified tab count logic to match requirements:
  - **Current Requests tab**: Shows count of current requests
  - **Request History tab**: Removed count display
  - **Upcoming Scheduled Re-orders tab**: Shows only count of reorders with scheduled POs already generated (filters by next_reorder_date match with existing scheduled POs)
  - **Purchase Orders tab**: Shows count of open and placed orders only (excludes on-hold items)
  - **Purchase Orders sub-tabs**: Only "open", "placed", and "on_hold" tabs show counts; "received" and "cancelled" don't show counts

**Code location:**
- Tab count definitions: [restock-workflow-tabs.tsx lines 564-578](components/admin/restock/restock-workflow-tabs.tsx#L564-L578)
- Sub-tab count logic: [restock-workflow-tabs.tsx lines 839-843](components/admin/restock/restock-workflow-tabs.tsx#L839-L843)

**How to verify:**
1. Navigate to Admin > Restock Management
2. Check each tab shows appropriate count:
   - Current Requests: Shows number in tab badge
   - Request History: NO count badge displayed
   - Upcoming Scheduled Re-orders: Shows count (should match scheduled POs with next dates)
   - Purchase Orders: Shows only open/placed order count
3. Click into Purchase Orders to view sub-tabs:
   - Open, Placed, On Hold tabs show counts
   - Received and Cancelled tabs show NO counts

---

## Task 3: Add search box to each restock management tab ✅ COMPLETED

**What was implemented:**
- Added `tabSearchTerm` state to track search input across all tabs
- Added search input field with live-updating dropdown that appears when typing
- Implemented search filtering for each tab type using useMemo hooks:
  - **Current Requests**: Searches by request_number, supplier name, pharmacist name/email
  - **Request History**: Searches by request_number, supplier name, pharmacist name/email
  - **Upcoming Scheduled Re-orders**: Searches by supplier name, schedule_label, next_reorder_date
  - **Purchase Orders**: Searches by po_number, supplier name, status, source
- Search results show as clickable dropdown items (max 8 results)

**Code location:**
- Search input UI: [restock-workflow-tabs.tsx lines 689-728](components/admin/restock/restock-workflow-tabs.tsx#L689-L728)
- Search term state: [restock-workflow-tabs.tsx line 140](components/admin/restock/restock-workflow-tabs.tsx#L140)
- Search filter functions: [restock-workflow-tabs.tsx lines 616-651](components/admin/restock/restock-workflow-tabs.tsx#L616-L651)

**How to verify:**
1. Navigate to Admin > Restock Management
2. In any tab (Current Requests, Request History, Upcoming Scheduled Re-orders, Purchase Orders), you should see a search box below the tabs
3. Type a search term (e.g., supplier name, request number, PO number)
4. A dropdown should appear showing matching items
5. Click on any search result to navigate/select that item
6. Verify the search updates as you type (no need to press Enter)

---

## Task 4: Convert new restock order page to modal ✅ COMPLETED

**What was implemented:**
- Removed the dedicated `/admin/restock/new` page (now just redirects to `/admin/restock?newRestock=1`)
- Created new [restock-page-client.tsx](components/admin/restock/restock-page-client.tsx) client component that handles modal state
- Modal opens automatically when `newRestock=1` query parameter is present
- "New Restock Order" button on main restock page opens modal instead of navigating
- Modal design matches the page design with same form, styling, and layout
- Modal has close button (X) and can be closed by the Cancel button in the form
- After successful submission, modal closes and user is redirected to the restock request details

**Code location:**
- Modal component: [restock-page-client.tsx](components/admin/restock/restock-page-client.tsx)
- Page redirect: [app/admin/restock/new/page.tsx lines 1-8](app/admin/restock/new/page.tsx)
- Main restock page: [app/admin/restock/page.tsx lines 1-24](app/admin/restock/page.tsx)

**How to verify:**
1. Navigate to Admin > Restock Management
2. Click "New Restock Order" button in top right
3. Verify: A modal opens (not a page navigation) with the title "New Restock Order"
4. The form inside the modal should have the same layout as the old dedicated page
5. Fill in the form and click "Submit Restock Order"
6. Verify: The modal closes and you're redirected to the restock request details page
7. Navigate back to restock management and add `?newRestock=1` to the URL directly
8. Verify: The modal opens automatically on page load

---

## Task 5: Add print/download PDF buttons to purchase order details modal ✅ COMPLETED

**What was implemented:**
- Added "Print Purchase Order" button that opens a print dialog with formatted PO details
- Added "Download as PDF" button that shows the print dialog with a helpful message (user can choose "Save as PDF" in print dialog)
- Both buttons appear in the purchase order details modal above the items list
- Print functionality generates a formatted HTML document with PO number, supplier, status, total, and itemized table
- Buttons are organized in a two-column grid layout

**Code location:**
- Print/Download functions: [restock-workflow-tabs.tsx lines 545-567](components/admin/restock/restock-workflow-tabs.tsx#L545-L567)
- Button UI: [restock-workflow-tabs.tsx lines 1407-1416](components/admin/restock/restock-workflow-tabs.tsx#L1407-L1416)
- HTML generation: [restock-workflow-tabs.tsx lines 545-563](components/admin/restock/restock-workflow-tabs.tsx#L545-L563)

**How to verify:**
1. Navigate to Admin > Restock Management > Purchase Orders tab
2. Click on any purchase order to open the details modal
3. You should see "Print Purchase Order" and "Download as PDF" buttons near the top
4. Click "Print Purchase Order" - verify print dialog opens with formatted PO details
5. Click "Download as PDF" - verify print dialog opens with a toast message explaining how to save as PDF
6. In the print dialog, verify the document shows PO details and itemized table

---

## Task 6: Add CSV format requirements explanation in bulk upload modal ✅ COMPLETED

**What was implemented:**
- Added informational section in the bulk upload modal explaining CSV format requirements
- Section includes:
  - "Keep only the columns you intend to map before uploading"
  - "Do not include commas in names or numbers"
  - "Use a clean header row with clear column names"
- Section has indigo/blue styling (bg-indigo-50, border-indigo-200) to make it stand out
- Appears at the top of the modal content area before the upload form

**Code location:**
- CSV format requirements section: [suppliers-list.tsx lines 1207-1216](components/admin/restock/suppliers-list.tsx#L1207-L1216)

**How to verify:**
1. Navigate to Admin > Restock Management > Manage Suppliers
2. Select a supplier and click to expand it
3. Click the "+" icon to add items or look for "Bulk Upload & Link Items" button
4. The modal should open
5. You should see a blue/indigo information box at the top with CSV format requirements
6. Verify all three requirements are listed:
   - Keep only intended columns
   - No commas in names/numbers
   - Clean header row

---

## Summary
All 6 tasks have been completed as per the commit 7a240cbe4c62851ab00971b600fb7618350654b3. The implementation includes proper UX enhancements, clear count logic, search functionality, and user guidance for bulk uploads.