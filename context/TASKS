1. the ability to hold items should be present in purchase order details modal

2. restock management tabs, request history tab does not need to count requests. upcoming scheduled re-orders tab should only count reorders that have a scheduled PO already generated. purchase orders tab only needs to count open and placed orders. tabs under the purchase orders tab, received and cancelled don't need to show a count, only open, placed and on hold tabs should display a count.

3. add a search box to each restock management tab that displays a dropdown with requests from that tab that contain the search term being searched for (should update while typing)

4. convert the new restock order page into a modal with the same design as the page (the modal should be a replica of the page), when the new restock order button is used it should open the modal instead of the page

5. purchase order details modal needs a button for printing the purchase order and one for downloading the purchase order as pdf

6. in the bulk upload modal for linking items in supplies management, add a section that explains the required format for csv files, the user should leave only the columns that they intend to map in the file and should not include commas in names and numbers