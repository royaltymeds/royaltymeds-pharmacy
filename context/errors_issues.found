1. the logic for the partially received feature is slightly flawed, the logic is correct when partially receiving and leaving the PO in the placed status, however when partially receiving but changing the status to received, it does not mean that all the items were received, only what was input as received was received so that impartial amount is the amount that should still show as received even if when asked in the modal we opt to mark the PO as received