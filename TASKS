1. the manage suppliers page, the cards showing the list of suppliers, make them expandable cards, when collapsed they should show minimal details (this is to minimize clutter), when expanded they should show the full details and have a button to view linked items that opens the linked items modal

2. the suppliers linked items modal, it should display a list of items (paginated with 20 per page) the items should be expandable, when an item is clicked that is when you should get the option to edit that item, this again is to avoid clutter and to keep it simple

3. when adding a new restock order, the add items section needs to show the list of items paginated, with up to 15 items per page in its paginated section

4. on all restock pages and modals where we have used pagination, we need a seacrch box to be able to search and select items for the purpose needed in each of those cases, also on POs when adding supplier items from edit or creation modal we need to have search there as well

5. when a manual PO is requested, the card on the upcoming scheduled re-orders should not be linked to it so as to change the generate po button to view po, there is still a need to generate po based on the schedule even if a manual PO exists for the supplier, manual POs and scheduled re-orders may follow different workflows

6. currently the restock pages that invoke a confirmation message, they are using the browser default message box, instead design a reusable modal component that they can all use for confirmation messages