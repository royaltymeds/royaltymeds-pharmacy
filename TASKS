1. the current request tab, it should display the list of suppliers (not with the current requests), when a supplier is clicked/selected a modal should open (use our preferred modal design), that modal should have a paginated list of current requests (same details on the cards as they have now) when a request card is clicked, it should open the request detail modal (same one we have now), we should be able to go back to the list of requests from that modal (the request history tab needs this exact design as well, same implementation)

2. the upcoming scheduled re-orders should be a list with the suppliers names, the cards should be expandable, the cards before being expanded should show the supplier name, frequency and upcoming date, when expanded it should show the supplier details and reveal the generate PO button / View PO button (so no longer use the modal)

3. the purchase order tab needs to also show the list of suppliers that have purchase orders, actually it needs different sections for different status POs, each section should be a tab of it's own, each section should show the list of suppliers with POs in it's status, when you click the supplier a modal should open showing you a list of POs for that supplier for that status, the list should be paginated

4. manual purchase orders should be able to have items linked to them even after the PO has been created

5. the manage suppliers page should also show first the list of suppliers, then when the supplier is clicked it should show a modal with pagination that lists the linked items, the linked items should be able to be edited